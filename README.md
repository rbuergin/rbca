R package for rating-based conjoint analysis

# Installation

To install this package directly from GitHub, use the `remotes` package:

```r
# Install remotes if you don't have it yet
install.packages("remotes")

# Install the package from GitHub
remotes::install_github("username/reponame")
